import * as path from 'path'

const ROOT_DIR = path.join(__dirname, '..')
const DATA_DIR = path.join(ROOT_DIR, 'data')
const ANIME_QUIZ_DATA_DIR = path.join(ROOT_DIR, '..', 'server', 'data')

export {DATA_DIR, ANIME_QUIZ_DATA_DIR}
